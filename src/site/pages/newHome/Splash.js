import React from "react";

const Splash = () => {
    return (
        <div className="full-screen splash-screen isLoading">
            Loading.... Waiting a monment
        </div>
    );
};

export default Splash;
